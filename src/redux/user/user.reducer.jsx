const INITITAL_STATE = {
    currentUser: null
}


const userReducer = (state = INITITAL_STATE, action) =>{
switch (action.type) {
    case '':
        
        break;

    default:
        break;
}
}